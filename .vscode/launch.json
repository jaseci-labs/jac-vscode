{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension (Jac build)",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/.jac/vscode"
      ],
      "preLaunchTask": "jac: build extension"
    }
  ]
}
